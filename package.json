{
  "name": "fdk-rdf-generator",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/build.ts",
    "generate": "bun run src/generate.ts",
    "publish": "bun run src/publish.ts"
  }
}
